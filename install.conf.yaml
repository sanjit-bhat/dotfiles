---
- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.gitconfig:
    ~/.inputrc:
    ~/.p10k.zsh:
    ~/.tmux.conf:
    ~/.vim:
    ~/.vimrc:
    ~/.z.sh:
    ~/.zsh:
    ~/.zshrc:

- create:

- shell:
    - [git submodule update --init --recursive, Installing submodules]
