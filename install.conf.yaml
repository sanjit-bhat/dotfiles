---
- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.fzf: zsh/plugins/fzf
    ~/.fzf.zsh:
    ~/.gitconfig:
    ~/.inputrc:
    ~/.p10k.zsh:
    ~/.tmux.conf:
    ~/.vim:
    ~/.vimrc:
    ~/.zsh:
    ~/.zshrc:

- create:

- shell:
    - [git submodule update --init --recursive, Installing submodules]
