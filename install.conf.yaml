---
- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.gitconfig:
    ~/.inputrc:
    ~/.p10k.zsh:
    ~/.ssh/config: 'ssh_config'
    ~/.tmux.conf:
    ~/.vim:
    ~/.vimrc:
    ~/.z.sh:
    ~/.zsh:
    ~/.zshrc:

- create:

- shell:
    - [git submodule update --init --recursive, Installing submodules]
