[user]
    name = Sanjit Bhat
    email = sanjit.bhat@gmail.com
    signingkey = ~/.ssh/id_ed25519.pub
[core]
    autocrlf = input
    editor = nvim
    excludesfile = ~/.gitignore
[color]
    ui = true
[branch]
    autosetuprebase = always
[pull]
    rebase = true
[push]
    default = tracking
    autoSetupRemote = true
[commit]
    gpgsign = true
[init]
    defaultBranch = main
[gpg]
    format = ssh
[alias]
    ad = add
    br = branch -v
    ci = commit -m
    cl = clone
    co = checkout
    df = diff
    # format: "<hash>|<date>|<committer>|<title>".
    # there's a bug that results in spurious vert bars at line ends.
    # i can't find a way to fix it while still preserving these things:
    # 1) custom format, 2) file names, 3) graph view.
    lg = log --all --graph --name-only --pretty="format:%C(auto)%h|%as|%cn|%s"
    pl = pull
    pu = push
    rb = rebase
    ro = remote
    rt = restore
    rts = restore --staged
    sm = submodule
    ss = status
    st = stash
